// Problem: Implement `bothStringsAndEqual`.
// Return true only if:
// - a and b are both strings
// - and they are equal.

export function bothStringsAndEqual(
  a: string | number,
  b: string | boolean,
): boolean {
  // TODO: implement using typeof + equality
  throw new Error("Not implemented");
}
